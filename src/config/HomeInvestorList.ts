// External Components
import { useTranslation } from "react-i18next";

const { t } = useTranslation();

const MediaList = {
  top: [
    {
      icon: 'static/assets/website/investors/raw/jump.svg',
      id: 'jump',
      title: null,
      subtitle: 'Jump Capital',
      alt: 'Push Investor - Jump',
    },
    
  ],
  bottom: [
  
  ]
};

export default MediaList;