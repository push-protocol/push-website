---
id: docs-notifications-ui-components-integrate-subscription-manager-widget
title: Integrating Subscription Manager Widget
hide_title: true
slug: ./subscription-manager-widget
displayed_sidebar: pushNotificationSidebar
sidebar_position: 2
image: "/assets/docs/previews/docs_notifications_ui_components_integrate_subscription_manager_widget--integrating_subscription_manager_widget.png"
---

# SubscriptionManagerWidget

Effortless Modal Integration: Add a modal to integrate managing subscription to a channel.

Subscription and managing settings become effortless tasks with this modal component, removing the need to worry about its functionality. By merely providing an onClose function and the channelAddress to the modal, you can seamlessly integrate it into your application. This flexibility enables you to subscribe or unsubscribe from any channel without any difficulties.

<head>
  <title>SubscriptionManager | Push Notifications | Push Documentation</title>
</head>

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { ModalContainer } from '@site/src/css/SharedStyling';

## Usage

<Tabs className="codetabs" groupId="code-examples">
  <TabItem  value="react" attributes={{className: "codetab react"}} default>

```typescript
import { SubscriptionManager } from "@pushprotocol/uiweb";

const SubscriptionManagerTest = () => {
  return (
    <div>
     <SubscriptionManager
       channelAddress="eip155:1:0xB88460Bb2696CAb9D66013A05dFF29a28330689D"
       onClose={()=>console.debug('on close modal')}
     />
    </div>
  );
};

export default SubscriptionManagerTest;
```

{" "}

<ModalContainer>
  <img
    src={
      require('/static/assets/docs/notifications/widgets/integrate-subscription-manager-widget.png')
        .default
    }
  />
</ModalContainer>

  </TabItem>
</Tabs>

---

### Customization parameters

| Param               | Type                  | Default | Remarks                                                                                                                                                                                          |
| ------------------- | --------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| _`onClose`_         | `function`            | -       | Function to perfom any task on clicking the cancel or cross button on the modal                                                                                                                  |
| _`channelAddress`_  | `string`              | -       | Address of the channel                                                                                                                                                                           |
| `modalBackground`   | `ModalBackgroundType` | -       | Default value is `"OVERLAY"`, decides the modal background, possible values are `"OVERLAY"` &#124 `"BLUR"` &#124 `"TRANSPARENT"`                                                                 |
| `modalPositionType` | `ModalPositionType`   | -       | Default value is `"GLOBAL"`, decides the modal position, it can be either relative to immediate parent(RELATIVE) or the entire screen(GLOBAL), possible values are `"RELATIVE"` &#124 `"GLOBAL"` |
| `autoconnect`       | `boolean`             | false   | Default value is false, connects to wallet after reload if true, or else disconnects the existing wallet connection                                                                              |

> Note: Parameters _`in this style`_ are mandatory.

> Note: Refer [WidgetUIProvider](/docs/notifications/ui-components/WidgetUIProvider/) for details on its paramters.

### Push SubscriptionManager Component live playground

```jsx live
// DO NOT FORGET TO IMPORT LIBRARIES
// NOT NEEDED HERE SINCE PLAYGROUND IMPORTS INTERNALLY
// import { WidgetUIProvider, SubscriptionManager,MODAL_POSITION_TYPE } from @pushprotocol/uiweb;
function App(props) {
  return (
    <>
      <WidgetUIProvider>
        <div style={{ height: '75vh', position: 'relative' }}>
          <SubscriptionManager
            channelAddress='eip155:1:0xB88460Bb2696CAb9D66013A05dFF29a28330689D'
            onClose={() => console.debug('on close modal')}
            modalPositionType={MODAL_POSITION_TYPE.RELATIVE}
          />
        </div>
      </WidgetUIProvider>
    </>
  );
}
```
