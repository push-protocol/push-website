---
id: docs-chat-ui-components-advanced-chatuiprovider
title: ChatUIProvider
hide_title: true
slug: ./chatuiprovider
displayed_sidebar: pushChatSidebar
sidebar_position: 1
---

# ChatUIProvider
Enhance State Management: Boost Your Component's Functionality by Wrapping it with ChatUIProvider.

<head>
  <title>ChatUIProvider | UI Components (@pushprotocol/uiweb) | Push Chat | Push Documentation</title>
</head>

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

## Usage
<Tabs className="codetabs" groupId="code-examples">
  <TabItem value="js" attributes={{className: "codetab js"}} default>

  ```typescript
  import { ChatUIProvider } from "@pushprotocol/uiweb";
  import { darkChatTheme } from "@pushprotocol/uiweb";

  export function App({ Component }) {
    return (
      <ChatUIProvider theme={darkChatTheme}>
        <Component />
      </ChatUIProvider>
    );
  }
  ```

  </TabItem>
</Tabs>

---

### Customization parameters
Customize the parameters as per your need and the chat component being used.

| Param         | Type            | Default | Remarks |
| ------------- | --------------- | ------- | -------- |
| `account`       | `string`         | -       | Supports wallet address or chain agnostic wallet address format|
| `pgpPrivatekey` | `string`          | -       | Decrypted pgp private key of the user |
| `theme`         | `IChatTheme`      | -       | [Theme object](/docs/chat/ui-components/Advanced/Theme/), it can be custom theme object, `lightChatTheme` `darkChatTheme` |
| _`children`_  | `React.ReactNode` | -       ||
| `env`           | `Env`             | -       |API env - 'prod' or 'staging'|
| `signer`        | `SignerType`          | -       |EthersV5 or Viem Signer|

> Note: Parameters _`in this style`_ are mandatory.
