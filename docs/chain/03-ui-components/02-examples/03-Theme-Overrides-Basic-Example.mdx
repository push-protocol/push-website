---
id: docs-chain-ui-components-example-theme-overrides-basic-example
title: Theme Overrides Example
hide_title: true
slug: ./theme-overrides-basic-example
displayed_sidebar: pushChainSidebar
sidebar_position: 3
---

## Example

This example demonstrates a basic wallet integration with the basic theme overrides.

## Live playground

```jsx live
// customPropMinimized='true'
// Import necessary components from @pushchain/ui-kit
import {
  PushUniversalWalletProvider,
  PushUniversalAccountButton,
  usePushWalletContext,
  CONSTANTS,
} from '@pushchain/ui-kit';

function App() {
  const walletConfig = {
    env: CONSTANTS.ENV.DEVNET,
    login: {
      email: true,
      google: true,
      wallet: {
        enabled: true,
      },
      appPreview: true,
    },
    modal: {
      loginLayout: CONSTANTS.LOGIN.SPLIT,
      connectedLayout: CONSTANTS.CONNECTED.HOVER,
      appPreview: true,
    },
  };

  const appMetadata = {
    logoUrl:
      'https://plus.unsplash.com/premium_photo-1746731481770-08b2f71661d0?q=80&w=2671&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D',
    title: 'Test App Title',
    description: 'Test App Description',
  };

  function WalletUI() {
    const { universalAddress } = usePushWalletContext();

    return (
      <div>
        <PushUniversalAccountButton />
      </div>
    );
  }

  return (
    <PushUniversalWalletProvider
      config={walletConfig}
      app={appMetadata}
      themeOverrides={{
        '--pw-core-bg-primary-color': '#FF474C',
        '--pw-core-font-family': 'Inter',
        '--pw-core-text-primary-color': '#fff',
        '--pw-core-text-secondary-color': '#F5F6F8',
        '--pw-core-btn-border-radius': '15px',
      }}
    >
      <WalletUI />
    </PushUniversalWalletProvider>
  );
}
```
