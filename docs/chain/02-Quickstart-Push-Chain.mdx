---
id: docs-chain-quickstart
title: Quickstart
hide_title: false
slug: ./quickstart
displayed_sidebar: pushChainSidebar
sidebar_position: 2
image: "/assets/docs/previews/docs_notifications--quickstart.png"
---

# Quickstart

Everything you will need to get up and running in 2 minutes or less!

<head>
  <title>Quickstart | Push Chain | Push Documentation</title>
</head>

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

### Installation

<Tabs className="codetabs" groupId="code-examples">
  <TabItem value="js" attributes={{ className: "codetab js" }} default>
    ```bash
    # Install Libraries
    npm install @pushprotocol/node-core viem dotenv
    ```
  </TabItem>
  <TabItem value="react" attributes={{ className: "codetab react" }} default>
    ```zsh
    # Install Libraries
    npm install @pushprotocol/node-core viem dotenv
    ```
  </TabItem>
  <TabItem value="reactnative" attributes={{ className: "codetab reactnative" }} default>
    ```zsh
    # Install Libraries
    npm install @pushprotocol/node-core viem dotenv
    ```
  </TabItem>
</Tabs>

### Import libraries

<Tabs className="codetabs" groupId="code-examples">
  <TabItem value="js" attributes={{ className: "codetab js" }} default>
    ```typescript
    // Import Push Chain SDK
    import {PushNetwork, CONSTANTS} from '@pushprotocol/node-core';

    // Import necessary dependencies
    import {hexToBytes} from 'viem'
    import {privateKeyToAccount} from 'viem/accounts'
    import {config} from 'dotenv'
    config()
    ```
  </TabItem>

  <TabItem value="react" attributes={{ className: "codetab react" }} default>
    ```typescript
    // Import Push Chain SDK
    import {PushNetwork, CONSTANTS} from '@pushprotocol/node-core';

    // Import necessary dependencies
    import {hexToBytes} from 'viem'
    import {privateKeyToAccount} from 'viem/accounts'
    import {config} from 'dotenv'
    config()
    ```
  </TabItem>

  <TabItem value="reactnative" attributes={{ className: "codetab reactnative" }} default>
    ```typescript
    // Import Push Chain SDK
    import {PushNetwork, CONSTANTS} from '@pushprotocol/node-core';

    // Import necessary dependencies
    import {hexToBytes} from 'viem'
    import {privateKeyToAccount} from 'viem/accounts'
    import {config} from 'dotenv'
    config()
    ```
  </TabItem>
</Tabs>

### Initialize Push Chain SDK

<Tabs className="codetabs" groupId="code-examples">
  <TabItem value="js" attributes={{ className: "codetab js" }} default>
    ```typescript
    // 'CONSTANTS.ENV.PROD' -> mainnet apps | 'CONSTANTS.ENV.STAGING' -> testnet apps
    const pushNetwork = await PushNetwork.initialize(CONSTANTS.ENV.STAGING);

    // Initializing viem Account. This will be used for signing when sending a transaction
    const senderEthereumPrivateKey = process.env.ETHEREUM_PRIVATE_KEY;
    if (!senderEthereumPrivateKey) throw new Error('Invalid private key');
    const ethereumSenderAccount = privateKeyToAccount(senderEthereumPrivateKey as `0x${string}`);
    ```
  </TabItem>

  <TabItem value="react" attributes={{ className: "codetab react" }} default>
    ```typescript
    // 'CONSTANTS.ENV.PROD' -> mainnet apps | 'CONSTANTS.ENV.STAGING' -> testnet apps
    const pushNetwork = await PushNetwork.initialize(CONSTANTS.ENV.STAGING);

    // Initializing viem Account. This will be used for signing when sending a transaction
    const senderEthereumPrivateKey = process.env.ETHEREUM_PRIVATE_KEY;
    if (!senderEthereumPrivateKey) throw new Error('Invalid private key');
    const ethereumSenderAccount = privateKeyToAccount(senderEthereumPrivateKey as `0x${string}`);
    ```
  </TabItem>

  <TabItem value="reactnative" attributes={{ className: "codetab reactnative" }} default>
    ```typescript
    // 'CONSTANTS.ENV.PROD' -> mainnet apps | 'CONSTANTS.ENV.STAGING' -> testnet apps
    const pushNetwork = await PushNetwork.initialize(CONSTANTS.ENV.STAGING);

    // Initializing viem Account. This will be used for signing when sending a transaction
    const senderEthereumPrivateKey = process.env.ETHEREUM_PRIVATE_KEY;
    if (!senderEthereumPrivateKey) throw new Error('Invalid private key');
    const ethereumSenderAccount = privateKeyToAccount(senderEthereumPrivateKey as `0x${string}`);
    ```
  </TabItem>
</Tabs>

### Send a Transaction

In this example we will be sending a transaction with category `MY_EMAIL_APP` and the following data:

```typescript
const email = {
  title: 'Hello old friend from Solana!',
  message: 'Greetings from Ethereum world.'
};
```

<Tabs className="codetabs" groupId="code-examples">
  <TabItem value="js" attributes={{ className: "codetab js" }} default>
    ```typescript
    // Create Signer object to sign messages
    const signer = {
      account: `eip155:1:${ethereumSenderAccount.address}`,
      signMessage: async (data: Uint8Array): Promise<Uint8Array> => {
        return hexToBytes(await ethereumSenderAccount.signMessage({message: {raw: data}}));
      }
    };

    // Create unsigned transaction
    const unsignedTransaction = pushNetwork.tx.createUnsigned(
      'MY_EMAIL_APP',
      // Solana address
      [ySYrGNLLJSK9hvGGpoxg8TzWfRe8ftBtDSMECtx2eJR],
      new TextEncoder().encode(JSON.stringify(email))
    );

    // Send transaction
    const transactionHash = await pushNetwork.tx.send(unsignedTransaction, signer);
    console.log(`Transaction submitted! ${transactionHash}`);
    ```
  </TabItem>

  <TabItem value="react" attributes={{ className: "codetab react" }} default>
    ```typescript
    // Create Signer object to sign messages
    const signer = {
      account: `eip155:1:${ethereumSenderAccount.address}`,
      signMessage: async (data: Uint8Array): Promise<Uint8Array> => {
        return hexToBytes(await ethereumSenderAccount.signMessage({message: {raw: data}}));
    }
  };

  // Create unsigned transaction
  const unsignedTransaction = pushNetwork.tx.createUnsigned(
    'MY_EMAIL_APP',
    // Solana address
    [ySYrGNLLJSK9hvGGpoxg8TzWfRe8ftBtDSMECtx2eJR],
    new TextEncoder().encode(JSON.stringify(email))
  );

  // Send transaction
  const transactionHash = await pushNetwork.tx.send(unsignedTransaction, signer);
  console.log(`Transaction submitted! ${transactionHash}`);
    ```
  </TabItem>

  <TabItem value="reactnative" attributes={{ className: "codetab reactnative" }} default>
    ```typescript
    // Create Signer object to sign messages
    const signer = {
      account: `eip155:1:${ethereumSenderAccount.address}`,
      signMessage: async (data: Uint8Array): Promise<Uint8Array> => {
        return hexToBytes(await ethereumSenderAccount.signMessage({message: {raw: data}}));
      }
    };

    // Create unsigned transaction
    const unsignedTransaction = pushNetwork.tx.createUnsigned(
      'MY_EMAIL_APP',
      // Solana address
      [ySYrGNLLJSK9hvGGpoxg8TzWfRe8ftBtDSMECtx2eJR],
      new TextEncoder().encode(JSON.stringify(email))
    );

    // Send transaction
    const transactionHash = await pushNetwork.tx.send(unsignedTransaction, signer);
    console.log(`Transaction submitted! ${transactionHash}`);
    ```
  </TabItem>
</Tabs>

### Fetch Transaction

<Tabs className="codetabs" groupId="code-examples">
  <TabItem value="js" attributes={{ className: "codetab js" }} default>
    ```typescript
    const transaction = await pushNetwork.tx.search('3e30a2a89f0122157832b3495a4e5e26e15c0e2123cbc2e17170ac6cdba5c6fa')
    ```
  </TabItem>

  <TabItem value="react" attributes={{ className: "codetab react" }} default>
    ```typescript
    const transaction = await pushNetwork.tx.search('3e30a2a89f0122157832b3495a4e5e26e15c0e2123cbc2e17170ac6cdba5c6fa')
    ```
  </TabItem>

  <TabItem value="reactnative" attributes={{ className: "codetab reactnative" }} default>
    ```typescript
    const transaction = await pushNetwork.tx.search('3e30a2a89f0122157832b3495a4e5e26e15c0e2123cbc2e17170ac6cdba5c6fa')
    ```
  </TabItem>
</Tabs>
